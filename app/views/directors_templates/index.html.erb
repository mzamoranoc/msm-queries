<!DOCTYPE html>
<html>
  <head>
    <title>Must See Movies - Directors</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
  </head>

  <body>

    <h1>List of all directors</h1>

    <hr>

    <table border="1">
      <tr>
        <th>ID</th>
        <th>Image</th>
        <th>Name</th>
        <th>Dob</th>
        <th></th>
      </tr>

      <% @directors.each do |director| %>
        <tr>
          <td><%= director.id %></td>

          <td>
            <% if director.image.present? %>
              <img src="<%= director.image %>" style="width:250px;">
            <% else %>
              No Image Available
            <% end %>
          </td>

          <td><%= director.name %></td>

          <td>
            <% if director.dob.present? %>
              <%= director.dob %>
            <% else %>
              Unknown
            <% end %>
          </td>

          <td>
            <a href="/directors/<%= director.id %>">
              Show details
            </a>
          </td>
        </tr>
      <% end %>
    </table>
  </body>
</html>
